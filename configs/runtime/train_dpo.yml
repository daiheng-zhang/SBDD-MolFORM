task: train-dpo
base_config: configs/training_dpo_kdd_confidence.yml
paths:
  repo_root: .
  data_root: ./data
  log_root: ./logs_diffusion
  output_root: ./outputs_sampling
  tmp_root: ./tmp
runtime:
  smoke: false
  tag: dpo_confidence_head
  run_name: KDD-Molform-DPO-Confidence
  train_report_iter: 10
  omp_num_threads: 16
inputs:
  dpo_data: null
  dpo_ref_ckpt: null
